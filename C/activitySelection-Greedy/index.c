#include<stdio.h>
#include<stdlib.h>
#define SIZE 5

struct Activity {
    int startTime;
    int finishTime;
};

void main() {   
    struct Activity *array = (struct array*)(malloc(SIZE * (sizeof (struct Activity))));
    
    addActivity(array, )
}